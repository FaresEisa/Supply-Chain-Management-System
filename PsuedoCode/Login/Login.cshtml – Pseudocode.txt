Login.cshtml – Pseudocode

Purpose:
UI form for users to enter login credentials.
Handles email and password input with a server-side POST.

Form Structure:
	-Form method: POST
	-Email input field (required)
	-Password input field (required)
	-Submit button labeled "Login"
	-Error message container (shown if login fails)